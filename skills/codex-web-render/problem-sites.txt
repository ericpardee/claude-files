# Problem sites — URLs that WebFetch cannot render.
#
# One URL substring per line. If a target URL CONTAINS any line below, skip
# WebFetch and render with Codex instead (scripts/render-url.sh).
# Lines starting with # and blank lines are ignored.
#
# Use the most specific stable pattern that identifies the problem: a bare
# domain (example.com) or a path prefix (example.com/app).

claude.ai/share
